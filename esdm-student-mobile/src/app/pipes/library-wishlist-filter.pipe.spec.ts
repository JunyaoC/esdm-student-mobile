import { LibraryWishlistFilterPipe } from './library-wishlist-filter.pipe';

describe('LibraryWishlistFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LibraryWishlistFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
