import { LibraryCategoryFilterPipe } from './library-category-filter.pipe';

describe('LibraryCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LibraryCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
